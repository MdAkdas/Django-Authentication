from django.db import models

# no need to add any custom model
# if we have to do user profiling like adding dp, address fields or any other info
# then we can create a profile model connect it to django's user model